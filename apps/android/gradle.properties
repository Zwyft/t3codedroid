org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Disable API modeling — AGP 7.3.1's D8 cannot parse the newer
# FakeDependency.jar config format; this skips that transform entirely.
android.enableApiModeling=false
android.suppressUnsupportedOptionWarnings=android.enableApiModeling
